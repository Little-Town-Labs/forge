# OpenAI Configuration
# Required for AI chat functionality and embeddings
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Pinecone Configuration  
# Required for vector database operations (optional - app works in demo mode without)
# Get your API key from: https://app.pinecone.io/
PINECONE_API_KEY=your_pinecone_api_key_here

# Pinecone Index Name
# The name of your Pinecone index for storing document embeddings
# Must be created with 1024 dimensions to match text-embedding-3-small
PINECONE_INDEX=your_pinecone_index_name

# Clerk Authentication Configuration
# Required for user authentication and session management
# Create a Clerk application at: https://clerk.com/
# Configure allowed domains in Clerk dashboard to match your deployment URLs

# Clerk Publishable Key
# Safe for client-side use (exposed to browser)
# Get from Clerk Dashboard > API Keys > Publishable key
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here

# Clerk Secret Key  
# KEEP SECURE - Never expose to client-side code
# Used for server-side operations and API authentication
# Get from Clerk Dashboard > API Keys > Secret key
CLERK_SECRET_KEY=your_clerk_secret_key_here

# Admin Configuration
# Simple admin system using email allowlist for managing user invitations
# Emails must match exactly with Clerk user email addresses (case-sensitive)
# Can be upgraded to role-based permissions system later

# Admin Emails
# Comma-separated list of email addresses with admin privileges
# Example: admin@company.com,manager@company.com
# These users can create/manage invitations and access admin dashboard
ADMIN_EMAILS=admin@company.com,manager@company.com

# Rate Limiting Configuration
# Controls invitation creation frequency to prevent abuse and accidental bulk sending
# Limits apply per admin user individually

# Rate limiting mode: redis, memory, or disabled (auto-detected if not set)
# - redis: Production-ready, requires REDIS_URL (recommended for production)
# - memory: Development only, does not work with multiple instances
# - disabled: No rate limiting, relies on Clerk's built-in limits only
RATE_LIMIT_MODE=redis

# Redis connection for distributed rate limiting (production recommended)
# Format: redis://username:password@host:port or redis://host:port
# Leave empty to auto-detect mode or disable Redis rate limiting
REDIS_URL=redis://localhost:6379

# Maximum invitations per minute per admin user (default: 5)
# Prevents rapid-fire invitation sending
MAX_INVITATIONS_PER_MINUTE=5

# Maximum invitations per hour per admin user (default: 20)  
# Prevents bulk invitation abuse
MAX_INVITATIONS_PER_HOUR=20